# i18n Support Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** ccw-cli の TUI / tips / `--help` / CLI メッセージを English / 日本語の 2 言語対応にし、`--lang` フラグ・`CCW_LANG` 環境変数・システム locale から言語を解決する。

**Architecture:** 新規 `internal/i18n` パッケージに API と検出ロジックを集約。翻訳テキストは `locales/{en,ja}.yaml` に分離し `go:embed` で同梱。`main()` 起動直後に `i18n.Init(flag)` を呼んでシングルトンを初期化、各サイトは `i18n.T(KeyXxx, args...)` で参照する。

**Tech Stack:** Go 1.25, `gopkg.in/yaml.v3`（新規依存）, `github.com/spf13/pflag`（既存）, `embed`（標準ライブラリ）

**Spec:** `docs/superpowers/specs/2026-04-26-i18n-design.md`

---

## File Structure

| Path | 役割 | 新規 / 変更 |
|---|---|---|
| `internal/i18n/i18n.go` | 公開 API: `Init` / `T` / `Current` の実装 | 新規 |
| `internal/i18n/keys.go` | `Lang` 型と `Key` 型、全 `Key` 定数の集約 | 新規 |
| `internal/i18n/detect.go` | システム locale 検出ロジック | 新規 |
| `internal/i18n/locales/en.yaml` | English 翻訳テキスト | 新規 |
| `internal/i18n/locales/ja.yaml` | 日本語翻訳テキスト | 新規 |
| `internal/i18n/i18n_test.go` | キー網羅 / 両 YAML の整合 / `%s` 数の整合 / `T` / `Init` のテスト | 新規 |
| `internal/i18n/detect_test.go` | フラグ × env × locale マトリクスのテスト | 新規 |
| `internal/tips/tips.go` | `defaults` を `[]i18n.Key` に変え、`Defaults()` で都度 `T()` 解決 | 変更 |
| `internal/tips/tips_test.go` | `TestMain` で `i18n.Init("en")` を呼ぶ | 変更 |
| `internal/cli/help.go` | `usage` 定数を削除し `T(KeyHelpUsage)` を返す | 変更 |
| `internal/cli/parse.go` | `--lang` フラグ追加 + 値検証 | 変更 |
| `internal/cli/parse_test.go` | `--lang` の検証テスト追加 | 変更 |
| `internal/picker/view.go` | 全 user-facing 文字列を `T()` に置換 | 変更 |
| `internal/picker/run.go` | フォールバック画面の文字列を `T()` に置換 | 変更 |
| `internal/picker/view_test.go` | `TestMain` で `i18n.Init("en")` を呼ぶ | 変更 |
| `internal/picker/model_test.go` | 同上 | 変更 |
| `cmd/ccw/main.go` | `cli.Parse` 直後に `i18n.Init(flags.Lang)` を呼ぶ | 変更 |
| `docs/assets/picker-demo.tape` | 先頭で `Env CCW_LANG en` を設定 | 変更 |
| `README.md` | `--lang` / `CCW_LANG` の節を Environment セクションに追加 | 変更 |
| `docs/README.ja.md` | 同等の節を日本語で追加 | 変更 |
| `go.mod` / `go.sum` | `gopkg.in/yaml.v3` を追加 | 変更 |

---

## Task 1: `gopkg.in/yaml.v3` 依存追加と i18n パッケージ scaffold

**Files:**

- Modify: `go.mod`, `go.sum`
- Create: `internal/i18n/keys.go`

- [ ] **Step 1: 依存追加**

```bash
go get gopkg.in/yaml.v3@latest
```

期待: `go.mod` に `gopkg.in/yaml.v3 vX.Y.Z` が追加され `go.sum` が更新される。

- [ ] **Step 2: `internal/i18n/keys.go` 作成**

```go
// Package i18n provides language detection and translated message lookup
// for ccw's user-facing output (TUI, tips, help, CLI messages).
package i18n

// Lang identifies an active language. Only "en" and "ja" are supported.
type Lang string

const (
 LangEN Lang = "en"
 LangJA Lang = "ja"
)

// Key is a stable identifier for a translatable message. Values match the
// dot-flattened path inside the locale YAML files.
type Key string
```

- [ ] **Step 3: ビルド確認**

Run: `go build ./internal/i18n/...`
期待: エラーなし。

- [ ] **Step 4: コミット**

```bash
git add go.mod go.sum internal/i18n/keys.go
git commit -m "feat(i18n): add yaml.v3 dependency and Lang/Key scaffold"
```

---

## Task 2: システム locale 検出（環境変数のみ）

**Files:**

- Create: `internal/i18n/detect.go`
- Create: `internal/i18n/detect_test.go`

- [ ] **Step 1: 失敗するテストを書く**

`internal/i18n/detect_test.go`:

```go
package i18n

import "testing"

func TestDetectFromEnv(t *testing.T) {
 cases := []struct {
  name string
  env  map[string]string
  want Lang
 }{
  {"empty defaults to EN", map[string]string{}, LangEN},
  {"LANG ja_JP.UTF-8", map[string]string{"LANG": "ja_JP.UTF-8"}, LangJA},
  {"LANG en_US.UTF-8", map[string]string{"LANG": "en_US.UTF-8"}, LangEN},
  {"LANG ja", map[string]string{"LANG": "ja"}, LangJA},
  {"LANG fr_FR.UTF-8 falls back to EN", map[string]string{"LANG": "fr_FR.UTF-8"}, LangEN},
  {"LC_MESSAGES wins over LANG", map[string]string{"LC_MESSAGES": "ja_JP", "LANG": "en_US"}, LangJA},
  {"LC_ALL wins over LC_MESSAGES", map[string]string{"LC_ALL": "ja_JP", "LC_MESSAGES": "en_US", "LANG": "en_US"}, LangJA},
  {"empty LC_ALL falls through", map[string]string{"LC_ALL": "", "LANG": "ja_JP"}, LangJA},
  {"modifier suffix stripped", map[string]string{"LANG": "ja_JP.UTF-8@bidi"}, LangJA},
  {"C locale defaults to EN", map[string]string{"LANG": "C"}, LangEN},
  {"POSIX locale defaults to EN", map[string]string{"LANG": "POSIX"}, LangEN},
 }
 for _, tc := range cases {
  t.Run(tc.name, func(t *testing.T) {
   lookup := func(k string) string { return tc.env[k] }
   if got := detectFromEnv(lookup); got != tc.want {
    t.Errorf("detectFromEnv(%v) = %q, want %q", tc.env, got, tc.want)
   }
  })
 }
}
```

- [ ] **Step 2: テストを走らせて失敗を確認**

Run: `go test ./internal/i18n/ -run TestDetectFromEnv -v`
期待: FAIL（`detectFromEnv` 未定義）。

- [ ] **Step 3: `internal/i18n/detect.go` を実装**

```go
package i18n

import (
 "os"
 "strings"
)

// detectFromEnv resolves a Lang from POSIX locale env vars, in priority order
// LC_ALL > LC_MESSAGES > LANG. Returns LangEN for any non-Japanese tag.
// lookup abstracts os.Getenv so tests can inject a fake environment.
func detectFromEnv(lookup func(string) string) Lang {
 for _, key := range []string{"LC_ALL", "LC_MESSAGES", "LANG"} {
  raw := lookup(key)
  if raw == "" {
   continue
  }
  if primaryTag(raw) == "ja" {
   return LangJA
  }
  return LangEN
 }
 return LangEN
}

// primaryTag normalizes a POSIX locale string ("ja_JP.UTF-8@modifier") to its
// primary language tag ("ja"). Empty string and "C"/"POSIX" return "".
func primaryTag(raw string) string {
 s := raw
 if i := strings.IndexByte(s, '@'); i >= 0 {
  s = s[:i]
 }
 if i := strings.IndexByte(s, '.'); i >= 0 {
  s = s[:i]
 }
 if i := strings.IndexByte(s, '_'); i >= 0 {
  s = s[:i]
 }
 s = strings.ToLower(s)
 if s == "c" || s == "posix" {
  return ""
 }
 return s
}

// detectSystem is the production wrapper that uses os.Getenv.
func detectSystem() Lang {
 return detectFromEnv(os.Getenv)
}
```

- [ ] **Step 4: テスト実行**

Run: `go test ./internal/i18n/ -run TestDetectFromEnv -v`
期待: すべて PASS。

- [ ] **Step 5: コミット**

```bash
git add internal/i18n/detect.go internal/i18n/detect_test.go
git commit -m "feat(i18n): add POSIX locale env detection"
```

---

## Task 3: YAML スケルトン + ロード関数

**Files:**

- Create: `internal/i18n/locales/en.yaml`
- Create: `internal/i18n/locales/ja.yaml`
- Modify: `internal/i18n/i18n.go`（新規作成）
- Modify: `internal/i18n/i18n_test.go`（新規作成）

- [ ] **Step 1: 最小 YAML を作成（後続タスクで増やす）**

`internal/i18n/locales/en.yaml`:

```yaml
meta:
  smoke: "english smoke value"
```

`internal/i18n/locales/ja.yaml`:

```yaml
meta:
  smoke: "日本語スモーク値"
```

- [ ] **Step 2: 失敗するテストを書く**

`internal/i18n/i18n_test.go`:

```go
package i18n

import "testing"

func TestLoadCatalog_BothLanguagesParse(t *testing.T) {
 en, err := loadCatalog(LangEN)
 if err != nil {
  t.Fatalf("loadCatalog(EN) error: %v", err)
 }
 if got := en["meta.smoke"]; got != "english smoke value" {
  t.Errorf("en[meta.smoke] = %q, want %q", got, "english smoke value")
 }
 ja, err := loadCatalog(LangJA)
 if err != nil {
  t.Fatalf("loadCatalog(JA) error: %v", err)
 }
 if got := ja["meta.smoke"]; got != "日本語スモーク値" {
  t.Errorf("ja[meta.smoke] = %q, want %q", got, "日本語スモーク値")
 }
}
```

- [ ] **Step 3: テストを走らせて失敗を確認**

Run: `go test ./internal/i18n/ -run TestLoadCatalog -v`
期待: FAIL（`loadCatalog` 未定義）。

- [ ] **Step 4: `internal/i18n/i18n.go` を実装**

```go
package i18n

import (
 "embed"
 "fmt"

 "gopkg.in/yaml.v3"
)

//go:embed locales/en.yaml locales/ja.yaml
var localesFS embed.FS

// loadCatalog parses the embedded YAML for lang into a flat map keyed by
// the dot-joined path ("picker.action.menu" etc.). Returns an error if the
// file is missing or malformed.
func loadCatalog(lang Lang) (map[string]string, error) {
 path := "locales/" + string(lang) + ".yaml"
 data, err := localesFS.ReadFile(path)
 if err != nil {
  return nil, fmt.Errorf("read %s: %w", path, err)
 }
 var raw map[string]any
 if err := yaml.Unmarshal(data, &raw); err != nil {
  return nil, fmt.Errorf("parse %s: %w", path, err)
 }
 out := make(map[string]string)
 flatten("", raw, out)
 return out, nil
}

// flatten walks a nested map[string]any and writes leaf string values into
// dst keyed by the dot-joined path. Non-string scalars cause a panic at
// startup, which surfaces YAML mistakes immediately during tests.
func flatten(prefix string, node map[string]any, dst map[string]string) {
 for k, v := range node {
  key := k
  if prefix != "" {
   key = prefix + "." + k
  }
  switch t := v.(type) {
  case string:
   dst[key] = t
  case map[string]any:
   flatten(key, t, dst)
  case map[any]any:
   converted := make(map[string]any, len(t))
   for kk, vv := range t {
    converted[fmt.Sprint(kk)] = vv
   }
   flatten(key, converted, dst)
  default:
   panic(fmt.Sprintf("i18n: unsupported YAML value at %q: %T", key, v))
  }
 }
}
```

- [ ] **Step 5: テスト実行**

Run: `go test ./internal/i18n/ -run TestLoadCatalog -v`
期待: PASS。

- [ ] **Step 6: コミット**

```bash
git add internal/i18n/i18n.go internal/i18n/i18n_test.go internal/i18n/locales/
git commit -m "feat(i18n): embed YAML catalogs and add loader"
```

---

## Task 4: `Init` の優先順位解決

**Files:**

- Modify: `internal/i18n/i18n.go`
- Modify: `internal/i18n/i18n_test.go`

- [ ] **Step 1: 失敗するテストを追加**

`internal/i18n/i18n_test.go` の末尾に追加:

```go
func TestInit_FlagWins(t *testing.T) {
 t.Setenv("LC_ALL", "ja_JP.UTF-8")
 t.Setenv("CCW_LANG", "ja")
 if err := Init("en"); err != nil {
  t.Fatalf("Init: %v", err)
 }
 if Current() != LangEN {
  t.Errorf("Current() = %q, want en", Current())
 }
}

func TestInit_EnvWinsOverLocale(t *testing.T) {
 t.Setenv("LC_ALL", "en_US.UTF-8")
 t.Setenv("CCW_LANG", "ja")
 if err := Init(""); err != nil {
  t.Fatalf("Init: %v", err)
 }
 if Current() != LangJA {
  t.Errorf("Current() = %q, want ja", Current())
 }
}

func TestInit_LocaleFallback(t *testing.T) {
 t.Setenv("LC_ALL", "ja_JP.UTF-8")
 t.Setenv("CCW_LANG", "")
 if err := Init(""); err != nil {
  t.Fatalf("Init: %v", err)
 }
 if Current() != LangJA {
  t.Errorf("Current() = %q, want ja", Current())
 }
}

func TestInit_DefaultEN(t *testing.T) {
 t.Setenv("LC_ALL", "")
 t.Setenv("LC_MESSAGES", "")
 t.Setenv("LANG", "")
 t.Setenv("CCW_LANG", "")
 if err := Init(""); err != nil {
  t.Fatalf("Init: %v", err)
 }
 if Current() != LangEN {
  t.Errorf("Current() = %q, want en", Current())
 }
}

func TestInit_InvalidFlagReturnsError(t *testing.T) {
 if err := Init("fr"); err == nil {
  t.Error("Init(\"fr\") returned nil, want error")
 }
}

func TestInit_InvalidEnvFallsThrough(t *testing.T) {
 t.Setenv("CCW_LANG", "fr")
 t.Setenv("LC_ALL", "ja_JP.UTF-8")
 if err := Init(""); err != nil {
  t.Fatalf("Init: %v", err)
 }
 if Current() != LangJA {
  t.Errorf("Current() = %q, want ja (env fr should fall through to locale)", Current())
 }
}
```

- [ ] **Step 2: テストを走らせて失敗を確認**

Run: `go test ./internal/i18n/ -run TestInit -v`
期待: FAIL（`Init` / `Current` 未定義）。

- [ ] **Step 3: `internal/i18n/i18n.go` に実装追加**

`internal/i18n/i18n.go` に append:

```go
import (
 // ... 既存の import に追加
 "os"
 "strings"
)

var (
 currentLang     = LangEN
 currentCatalog  map[string]string
)

// Init resolves the active language from (in priority order):
//
//  1. flagLang (e.g. from --lang); empty means "skip this source".
//  2. CCW_LANG env var.
//  3. POSIX locale (LC_ALL > LC_MESSAGES > LANG).
//  4. Default LangEN.
//
// flagLang is validated strictly — an unknown value returns an error.
// CCW_LANG values that aren't en/ja silently fall through to the locale.
// Init must be called once at startup before any T() lookup.
func Init(flagLang string) error {
 lang, err := resolveLang(flagLang, os.Getenv)
 if err != nil {
  return err
 }
 cat, err := loadCatalog(lang)
 if err != nil {
  return err
 }
 currentLang = lang
 currentCatalog = cat
 return nil
}

// Current returns the language resolved by the most recent Init call.
func Current() Lang { return currentLang }

func resolveLang(flagLang string, lookup func(string) string) (Lang, error) {
 if flagLang != "" {
  l, ok := parseLang(flagLang)
  if !ok {
   return "", fmt.Errorf("invalid --lang value: %q (expected 'en' or 'ja')", flagLang)
  }
  return l, nil
 }
 if env := lookup("CCW_LANG"); env != "" {
  if l, ok := parseLang(env); ok {
   return l, nil
  }
 }
 return detectFromEnv(lookup), nil
}

func parseLang(s string) (Lang, bool) {
 switch strings.ToLower(s) {
 case "en":
  return LangEN, true
 case "ja":
  return LangJA, true
 }
 return "", false
}
```

- [ ] **Step 4: テスト実行**

Run: `go test ./internal/i18n/ -run TestInit -v`
期待: すべて PASS。

- [ ] **Step 5: コミット**

```bash
git add internal/i18n/i18n.go internal/i18n/i18n_test.go
git commit -m "feat(i18n): add Init with flag/env/locale priority"
```

---

## Task 5: `T()` メッセージ取得 + フォーマット

**Files:**

- Modify: `internal/i18n/i18n.go`
- Modify: `internal/i18n/i18n_test.go`

- [ ] **Step 1: 失敗するテストを追加**

`internal/i18n/i18n_test.go`:

```go
func TestT_ReturnsTranslation(t *testing.T) {
 t.Setenv("CCW_LANG", "")
 if err := Init("ja"); err != nil {
  t.Fatalf("Init: %v", err)
 }
 if got := T("meta.smoke"); got != "日本語スモーク値" {
  t.Errorf("T(meta.smoke) = %q, want 日本語スモーク値", got)
 }
}

func TestT_FormatsArgs(t *testing.T) {
 if err := Init("en"); err != nil {
  t.Fatalf("Init: %v", err)
 }
 currentCatalog["test.fmt"] = "hello %s, count=%d"
 defer delete(currentCatalog, "test.fmt")
 if got := T("test.fmt", "world", 7); got != "hello world, count=7" {
  t.Errorf("T(test.fmt, world, 7) = %q", got)
 }
}

func TestT_UnknownKeyReturnsKey(t *testing.T) {
 if err := Init("en"); err != nil {
  t.Fatalf("Init: %v", err)
 }
 if got := T("does.not.exist"); got != "does.not.exist" {
  t.Errorf("T(missing) = %q, want %q", got, "does.not.exist")
 }
}
```

- [ ] **Step 2: テストを走らせて失敗を確認**

Run: `go test ./internal/i18n/ -run TestT -v`
期待: FAIL（`T` 未定義）。

- [ ] **Step 3: `T` 実装を `internal/i18n/i18n.go` に追加**

```go
// T returns the translation for key in the current language, formatted with
// fmt.Sprintf semantics. If the key is unknown, the key string itself is
// returned (fail-soft) so missing translations are visible but harmless.
func T(key Key, args ...any) string {
 tmpl, ok := currentCatalog[string(key)]
 if !ok {
  return string(key)
 }
 if len(args) == 0 {
  return tmpl
 }
 return fmt.Sprintf(tmpl, args...)
}
```

- [ ] **Step 4: テスト実行**

Run: `go test ./internal/i18n/ -v`
期待: すべて PASS。

- [ ] **Step 5: コミット**

```bash
git add internal/i18n/i18n.go internal/i18n/i18n_test.go
git commit -m "feat(i18n): add T(key, args...) lookup with sprintf formatting"
```

---

## Task 6: Key 定数集約 + parity チェック

このタスクで **すべての** `Key` 定数を Go 側に列挙し、CI で「Go 定数 ↔ en.yaml ↔ ja.yaml」が完全一致することを保証する。後続のタスクは「定数追加 + YAML 追加 + 呼び出し置換」というフォーマットで進める。

**Files:**

- Modify: `internal/i18n/keys.go`
- Modify: `internal/i18n/i18n_test.go`

- [ ] **Step 1: Key 定数を列挙**

`internal/i18n/keys.go` の末尾に追加:

```go
const (
 // tips
 KeyTipRename       Key = "tip.rename"
 KeyTipFromPR       Key = "tip.fromPR"
 KeyTipCleanAll     Key = "tip.cleanAll"
 KeyTipPassthrough  Key = "tip.passthrough"
 KeyTipResumeBadge  Key = "tip.resumeBadge"

 // help
 KeyHelpUsage Key = "help.usage"

 // picker footer
 KeyPickerFooterInstallGh Key = "picker.footer.installGh"
 KeyPickerFooterTip       Key = "picker.footer.tip"

 // picker action menu (after row selected)
 KeyPickerActionMenu Key = "picker.action.menu"

 // delete confirm
 KeyPickerDeleteConfirm Key = "picker.delete.confirm"

 // prune confirm
 KeyPickerPruneSingle    Key = "picker.prune.single"
 KeyPickerPruneBulkHead  Key = "picker.prune.bulkHead"
 KeyPickerPruneBulkLine  Key = "picker.prune.bulkLine"
 KeyPickerPruneBulkFoot  Key = "picker.prune.bulkFoot"

 // bulk filter screen
 KeyPickerBulkFilterHead  Key = "picker.bulk.filterHead"
 KeyPickerBulkFilterLine  Key = "picker.bulk.filterLine"
 KeyPickerBulkFilterKeys  Key = "picker.bulk.filterKeys"
 KeyPickerBulkFilterFoot  Key = "picker.bulk.filterFoot"

 // bulk confirm
 KeyPickerBulkConfirmHead Key = "picker.bulk.confirmHead"
 KeyPickerBulkPruneNote   Key = "picker.bulk.pruneNote"
 KeyPickerBulkDirtyWarn   Key = "picker.bulk.dirtyWarn"
 KeyPickerBulkConfirmYN   Key = "picker.bulk.confirmYN"

 // fallback (non-TUI) picker
 KeyFallbackHeader Key = "fallback.header"
 KeyFallbackLine   Key = "fallback.line"
 KeyFallbackNew    Key = "fallback.new"
 KeyFallbackQuit   Key = "fallback.quit"
 KeyFallbackPrompt Key = "fallback.prompt"
)

// AllKeys returns every Key constant defined above. Used by the parity test
// to verify that the YAML catalogs cover the full set.
func AllKeys() []Key {
 return []Key{
  KeyTipRename, KeyTipFromPR, KeyTipCleanAll, KeyTipPassthrough, KeyTipResumeBadge,
  KeyHelpUsage,
  KeyPickerFooterInstallGh, KeyPickerFooterTip,
  KeyPickerActionMenu,
  KeyPickerDeleteConfirm,
  KeyPickerPruneSingle, KeyPickerPruneBulkHead, KeyPickerPruneBulkLine, KeyPickerPruneBulkFoot,
  KeyPickerBulkFilterHead, KeyPickerBulkFilterLine, KeyPickerBulkFilterKeys, KeyPickerBulkFilterFoot,
  KeyPickerBulkConfirmHead, KeyPickerBulkPruneNote, KeyPickerBulkDirtyWarn, KeyPickerBulkConfirmYN,
  KeyFallbackHeader, KeyFallbackLine, KeyFallbackNew, KeyFallbackQuit, KeyFallbackPrompt,
 }
}
```

- [ ] **Step 2: parity / format-verb テストを追加**

`internal/i18n/i18n_test.go`:

```go
import (
 // 既存の "testing" に追加
 "regexp"
 "sort"
)

func TestCatalogParity_AllKeysPresentInBothLocales(t *testing.T) {
 en, err := loadCatalog(LangEN)
 if err != nil {
  t.Fatalf("load en: %v", err)
 }
 ja, err := loadCatalog(LangJA)
 if err != nil {
  t.Fatalf("load ja: %v", err)
 }
 wantKeys := make(map[string]bool, len(AllKeys()))
 for _, k := range AllKeys() {
  wantKeys[string(k)] = true
 }

 checkSetEquality(t, "en", wantKeys, keysOf(en))
 checkSetEquality(t, "ja", wantKeys, keysOf(ja))
}

func TestCatalogParity_FormatVerbsMatch(t *testing.T) {
 en, _ := loadCatalog(LangEN)
 ja, _ := loadCatalog(LangJA)
 verb := regexp.MustCompile(`%[+\-#0 ]*\d*\.?\d*[sdvqxXfgte%]`)
 for _, k := range AllKeys() {
  ev := verb.FindAllString(en[string(k)], -1)
  jv := verb.FindAllString(ja[string(k)], -1)
  if !equalSlices(ev, jv) {
   t.Errorf("key %q: en verbs %v, ja verbs %v (must match exactly in order and type)", k, ev, jv)
  }
 }
}

func keysOf(m map[string]string) map[string]bool {
 out := make(map[string]bool, len(m))
 for k := range m {
  out[k] = true
 }
 return out
}

func checkSetEquality(t *testing.T, label string, want, got map[string]bool) {
 t.Helper()
 var missing, extra []string
 for k := range want {
  if !got[k] {
   missing = append(missing, k)
  }
 }
 for k := range got {
  if !want[k] {
   extra = append(extra, k)
  }
 }
 sort.Strings(missing)
 sort.Strings(extra)
 if len(missing) > 0 {
  t.Errorf("%s catalog missing keys: %v", label, missing)
 }
 if len(extra) > 0 {
  t.Errorf("%s catalog has extra keys not in AllKeys(): %v", label, extra)
 }
}

func equalSlices(a, b []string) bool {
 if len(a) != len(b) {
  return false
 }
 for i := range a {
  if a[i] != b[i] {
   return false
  }
 }
 return true
}
```

- [ ] **Step 3: テストを走らせて失敗を確認（YAML がまだ空なので失敗するはず）**

Run: `go test ./internal/i18n/ -run TestCatalogParity -v`
期待: FAIL（多数の missing keys 報告）。

- [ ] **Step 4: `internal/i18n/locales/en.yaml` を全キー対応に書き換え**

```yaml
meta:
  smoke: "english smoke value"

tip:
  rename: "Worktree name = session name; renaming with /rename is fine, ccw doesn't track it"
  fromPR: "claude --from-pr <number> resumes a PR-linked session directly"
  cleanAll: "--clean-all sweeps pushed worktrees in bulk"
  passthrough: "ccw -- --model <id> passes flags through to claude"
  resumeBadge: "The RESUME badge is derived from ~/.claude/projects/"

help:
  usage: |
    Usage: ccw [options] [-- <claude-args>...]

    Options:
      -n, --new            Always start a new worktree (skip picker)
      -s, --superpowers    Inject superpowers preamble (implies -n)
      -v, --version        Show version
      -h, --help           Show this help
          --lang=<en|ja>   Force output language (overrides CCW_LANG and locale)

    Bulk delete:
          --clean-all        Bulk delete mode
          --status=<filter>  all | pushed | local-only | dirty (default: all)
          --force            Delete dirty worktrees with --force
          --dry-run          Print targets and exit
      -y, --yes              Skip confirmation prompts (--clean-all, -s plugin install)

    Arguments after `--` are forwarded to `claude` verbatim.

    Environment:
      NO_COLOR=1           Disable colored output
      CCW_DEBUG=1          Verbose debug logging
      CCW_LANG=en|ja       Force output language

    Exit codes:
      0  success
      1  user error / cancellation
      *  passthrough from `claude`

    Repository: https://github.com/tqer39/ccw-cli

picker:
  footer:
    installGh: "💡 Install gh to see PR titles here"
    tip: "💡 Tip: %s"
  action:
    menu: |
      Selected: %s (%s)
      Path:     %s

      What to do?
        [r] run    — start claude in this worktree
        [d] delete — remove the worktree
        [b] back   — return to list
        [q] quit   — cancel
  delete:
    confirm: |
      Delete worktree %s?
        path:   %s
        status: %s

      This will run: %s

      Confirm? [y/N]
  prune:
    single: |
      Prune worktree %s?
        path:   %s

      This will run: git worktree prune

      Confirm? [y/N]
    bulkHead: "Prune %d prunable worktrees? (git worktree prune removes all of them at once)\n\n"
    bulkLine: "  %s %s\n"
    bulkFoot: "\nThis will run: git worktree prune\n\nConfirm? [y/N]\n"
  bulk:
    filterHead: "Select statuses to delete (toggle):\n\n"
    filterLine: "  %s %s\n"
    filterKeys: "  [p] pushed  [l] local-only  [d] dirty  [a] clear\n"
    filterFoot: "  [enter] confirm  [q/esc] back\n"
    confirmHead: "Delete %d worktrees?\n\n"
    pruneNote: "\nℹ Prunable entries will be cleaned up via `git worktree prune` after the removals.\n"
    dirtyWarn: |

      ⚠ Dirty worktrees are included. `git worktree remove --force` is required.
        [y] yes (include dirty, use --force)
        [s] skip dirty (remove clean only)
        [N] cancel
    confirmYN: "\nConfirm? [y/N]\n"

fallback:
  header: "Select a worktree:"
  line: "  %d) %s  (%s)  %s  %s\n"
  new: "  n) new"
  quit: "  q) quit"
  prompt: "#? "
```

- [ ] **Step 5: `internal/i18n/locales/ja.yaml` を全キー対応に書き換え**

```yaml
meta:
  smoke: "日本語スモーク値"

tip:
  rename: "worktree 名 = session 名。/rename で改名しても ccw は追跡しません"
  fromPR: "claude --from-pr <番号> で PR と紐づいた session を直接 resume できます"
  cleanAll: "--clean-all で push 済み worktree を一括削除できます"
  passthrough: "ccw -- --model <id> のように -- 以降は claude にそのまま渡されます"
  resumeBadge: "RESUME バッジは ~/.claude/projects/ から判定しています"

help:
  usage: |
    使い方: ccw [オプション] [-- <claude への引数>...]

    オプション:
      -n, --new            常に新しい worktree で開始 (picker をスキップ)
      -s, --superpowers    superpowers プリアンブルを注入 (-n を含む)
      -v, --version        バージョン表示
      -h, --help           このヘルプを表示
          --lang=<en|ja>   出力言語を強制 (CCW_LANG・locale より優先)

    一括削除:
          --clean-all        一括削除モード
          --status=<filter>  all | pushed | local-only | dirty (デフォルト: all)
          --force            dirty な worktree を --force で削除
          --dry-run          対象を表示して終了
      -y, --yes              確認プロンプトをスキップ (--clean-all, -s plugin install)

    `--` 以降の引数は `claude` にそのまま渡されます。

    環境変数:
      NO_COLOR=1           色付き出力を無効化
      CCW_DEBUG=1          詳細デバッグログ
      CCW_LANG=en|ja       出力言語を強制

    終了コード:
      0  成功
      1  ユーザーエラー / キャンセル
      *  `claude` の終了コードをそのまま返す

    リポジトリ: https://github.com/tqer39/ccw-cli

picker:
  footer:
    installGh: "💡 gh をインストールすると PR タイトルを表示できます"
    tip: "💡 Tip: %s"
  action:
    menu: |
      選択中: %s (%s)
      パス:    %s

      操作を選択してください:
        [r] 実行  — この worktree で claude を起動
        [d] 削除  — この worktree を削除
        [b] 戻る  — 一覧に戻る
        [q] 終了  — キャンセル
  delete:
    confirm: |
      worktree %s を削除しますか？
        path:   %s
        状態:   %s

      実行: %s

      確認 [y/N]
  prune:
    single: |
      worktree %s を prune しますか？
        path:   %s

      実行: git worktree prune

      確認 [y/N]
    bulkHead: "prune 対象の worktree が %d 件あります（git worktree prune で一括削除されます）\n\n"
    bulkLine: "  %s %s\n"
    bulkFoot: "\n実行: git worktree prune\n\n確認 [y/N]\n"
  bulk:
    filterHead: "削除対象の状態を選択してください (トグル):\n\n"
    filterLine: "  %s %s\n"
    filterKeys: "  [p] pushed  [l] local-only  [d] dirty  [a] クリア\n"
    filterFoot: "  [enter] 確定  [q/esc] 戻る\n"
    confirmHead: "%d 件の worktree を削除しますか？\n\n"
    pruneNote: "\nℹ prune 対象は削除後に `git worktree prune` で整理されます。\n"
    dirtyWarn: |

      ⚠ dirty な worktree が含まれます。`git worktree remove --force` が必要です。
        [y] はい（dirty も含めて --force で削除）
        [s] dirty をスキップ（clean のみ削除）
        [N] キャンセル
    confirmYN: "\n確認 [y/N]\n"

fallback:
  header: "worktree を選択してください:"
  line: "  %d) %s  (%s)  %s  %s\n"
  new: "  n) 新規"
  quit: "  q) 終了"
  prompt: "#? "
```

- [ ] **Step 6: `meta.smoke` キー対応として `AllKeys()` も忘れず再確認**

`AllKeys()` には `meta.smoke` を含めない（テスト用一時キーは parity チェック対象外にする）。
parity テストは `wantKeys` を `AllKeys()` から組み立てているので、YAML に `meta.smoke` があると "extra keys" で失敗する。
そこで `i18n_test.go` の `checkSetEquality` 呼び出しを smoke キー除外に修正:

```go
// Step 2 で書いたテストの該当部分を以下に差し替え:
func TestCatalogParity_AllKeysPresentInBothLocales(t *testing.T) {
 en, err := loadCatalog(LangEN)
 if err != nil {
  t.Fatalf("load en: %v", err)
 }
 ja, err := loadCatalog(LangJA)
 if err != nil {
  t.Fatalf("load ja: %v", err)
 }
 want := make(map[string]bool, len(AllKeys()))
 for _, k := range AllKeys() {
  want[string(k)] = true
 }
 // meta.smoke はテスト専用キーで AllKeys() に含まれないため除外
 delete(en, "meta.smoke")
 delete(ja, "meta.smoke")

 checkSetEquality(t, "en", want, keysOf(en))
 checkSetEquality(t, "ja", want, keysOf(ja))
}
```

- [ ] **Step 7: テスト実行**

Run: `go test ./internal/i18n/ -v`
期待: すべて PASS（parity・format-verb 含む）。

- [ ] **Step 8: コミット**

```bash
git add internal/i18n/keys.go internal/i18n/i18n_test.go internal/i18n/locales/en.yaml internal/i18n/locales/ja.yaml
git commit -m "feat(i18n): add full Key set with en/ja catalogs and parity tests"
```

---

## Task 7: tips パッケージの移行

**Files:**

- Modify: `internal/tips/tips.go`
- Modify: `internal/tips/tips_test.go`

- [ ] **Step 1: テスト先行 — `tips_test.go` を更新**

`internal/tips/tips_test.go` の冒頭を以下に書き換え:

```go
package tips

import (
 "strings"
 "testing"

 "github.com/tqer39/ccw-cli/internal/i18n"
)

func TestMain(m *testing.M) {
 if err := i18n.Init("en"); err != nil {
  panic(err)
 }
 m.Run()
}
```

その他のテスト本体（`TestPickRandom_FromDefaultSet` 等）はそのまま残す。
`TestPickRandom_FromDefaultSet` の期待文字列は YAML の英語値と一致するので変更不要（`Defaults()` が翻訳済みを返す前提）。

- [ ] **Step 2: テストを走らせて失敗を確認**

Run: `go test ./internal/tips/ -v`
期待: FAIL（i18n 未使用なので import unused、または `Defaults()` が古い文字列を返す）。

- [ ] **Step 3: `internal/tips/tips.go` を書き換え**

```go
// Package tips provides short rotating tip strings shown in the picker footer.
package tips

import (
 "math/rand/v2"

 "github.com/tqer39/ccw-cli/internal/i18n"
)

var keys = []i18n.Key{
 i18n.KeyTipRename,
 i18n.KeyTipFromPR,
 i18n.KeyTipCleanAll,
 i18n.KeyTipPassthrough,
 i18n.KeyTipResumeBadge,
}

// Defaults returns the current language's tip strings.
func Defaults() []string {
 out := make([]string, len(keys))
 for i, k := range keys {
  out[i] = i18n.T(k)
 }
 return out
}

// PickRandom returns a single tip selected deterministically from seed.
func PickRandom(seed uint64) string {
 return pickFrom(Defaults(), seed)
}

func pickFrom(set []string, seed uint64) string {
 if len(set) == 0 {
  return ""
 }
 r := rand.New(rand.NewPCG(seed, seed^0x9E3779B97F4A7C15))
 return set[r.IntN(len(set))]
}
```

- [ ] **Step 4: テスト実行**

Run: `go test ./internal/tips/ -v`
期待: すべて PASS。

- [ ] **Step 5: コミット**

```bash
git add internal/tips/tips.go internal/tips/tips_test.go
git commit -m "refactor(tips): resolve tip strings via i18n.T"
```

---

## Task 8: cli/help.go と --lang フラグ

**Files:**

- Modify: `internal/cli/help.go`
- Modify: `internal/cli/parse.go`
- Modify: `internal/cli/parse_test.go`

- [ ] **Step 1: parse_test.go に `--lang` テストを追加**

`internal/cli/parse_test.go` の末尾に追加（`testing` import は既存）:

```go
func TestParse_LangFlag(t *testing.T) {
 cases := []struct {
  name    string
  args    []string
  want    string
  wantErr bool
 }{
  {"default empty", []string{}, "", false},
  {"--lang=en", []string{"--lang=en"}, "en", false},
  {"--lang=ja", []string{"--lang=ja"}, "ja", false},
  {"--lang en", []string{"--lang", "ja"}, "ja", false},
  {"--lang invalid still parses (Init validates)", []string{"--lang=fr"}, "fr", false},
 }
 for _, tc := range cases {
  t.Run(tc.name, func(t *testing.T) {
   got, err := Parse(tc.args)
   if (err != nil) != tc.wantErr {
    t.Fatalf("err = %v, wantErr = %v", err, tc.wantErr)
   }
   if got.Lang != tc.want {
    t.Errorf("Lang = %q, want %q", got.Lang, tc.want)
   }
  })
 }
}
```

注意: `--lang` の値検証は `i18n.Init()` 側に集約する設計。`cli.Parse` は値をそのまま `Flags.Lang` に詰めるだけ（理由: cli パッケージが i18n に依存しないようにし、循環参照を避ける）。

- [ ] **Step 2: テストを走らせて失敗を確認**

Run: `go test ./internal/cli/ -run TestParse_LangFlag -v`
期待: FAIL（`Lang` フィールド未定義）。

- [ ] **Step 3: `internal/cli/parse.go` に `--lang` を追加**

`Flags` 構造体に `Lang string` を追加。`fs.StringVar` 行を `--yes` の直後に追加:

```go
// Flags struct に追加:
type Flags struct {
 Help         bool
 Version      bool
 NewWorktree  bool
 Superpowers  bool
 CleanAll     bool
 StatusFilter string
 Force        bool
 DryRun       bool
 AssumeYes    bool
 Lang         string
 Passthrough  []string
}

// fs.BoolVarP(&f.AssumeYes, ...) の直後に追加:
fs.StringVar(&f.Lang, "lang", "", "force output language: en | ja")
```

- [ ] **Step 4: parse テスト実行**

Run: `go test ./internal/cli/ -run TestParse_LangFlag -v`
期待: すべて PASS。

- [ ] **Step 5: `internal/cli/help.go` を i18n に置換**

```go
package cli

import (
 "fmt"
 "io"

 "github.com/tqer39/ccw-cli/internal/i18n"
)

// PrintHelp writes the localized usage string to w.
func PrintHelp(w io.Writer) {
 _, _ = fmt.Fprint(w, i18n.T(i18n.KeyHelpUsage))
}
```

- [ ] **Step 6: cli パッケージ全テスト実行**

Run: `go test ./internal/cli/ -v`
期待: すべて PASS（`PrintHelp` 系のテストがあれば、出力が i18n から来る前提で動く。テスト先頭に `i18n.Init("en")` を呼ぶ TestMain が必要であれば追加）。

もし `cli` パッケージのテストが何らか PrintHelp を呼ぶ場合は `TestMain` を追加:

```go
// internal/cli/parse_test.go の冒頭に追加:
import "github.com/tqer39/ccw-cli/internal/i18n"

func TestMain(m *testing.M) {
 if err := i18n.Init("en"); err != nil {
  panic(err)
 }
 m.Run()
}
```

- [ ] **Step 7: コミット**

```bash
git add internal/cli/parse.go internal/cli/parse_test.go internal/cli/help.go
git commit -m "feat(cli): add --lang flag and route help via i18n"
```

---

## Task 9: picker/view.go の移行

**Files:**

- Modify: `internal/picker/view.go`
- Modify: `internal/picker/view_test.go`
- Modify: `internal/picker/model_test.go`

- [ ] **Step 1: 両テストファイルに `TestMain` を追加**

`internal/picker/view_test.go` 冒頭の import に `"github.com/tqer39/ccw-cli/internal/i18n"` を追加し、トップレベルに:

```go
func TestMain(m *testing.M) {
 if err := i18n.Init("en"); err != nil {
  panic(err)
 }
 m.Run()
}
```

`internal/picker/model_test.go` には既に `TestMain` がある場合はその中で `i18n.Init` を呼ぶ。無ければ同じものを追加（package テストは package 単位で `TestMain` が 1 つ必要）。

**注意**: `TestMain` は package 内で 1 つだけ。`view_test.go` か `model_test.go` のどちらか片方にだけ書く。`view_test.go` 側に置く。

- [ ] **Step 2: 失敗を確認（既存テストが英語固定で動くことを保証する変更なので、ここでは既存テストが PASS のままを期待）**

Run: `go test ./internal/picker/ -v`
期待: PASS（変更前と同じ）。

- [ ] **Step 3: `internal/picker/view.go` を全置換**

```go
package picker

import (
 "fmt"
 "strings"

 tea "charm.land/bubbletea/v2"
 "charm.land/lipgloss/v2"
 "github.com/tqer39/ccw-cli/internal/i18n"
 "github.com/tqer39/ccw-cli/internal/worktree"
)

// View implements tea.Model.
func (m Model) View() tea.View {
 v := tea.NewView(m.viewContent())
 v.AltScreen = true
 return v
}

func (m Model) viewContent() string {
 switch m.state {
 case stateList:
  base := m.list.View()
  footer := ""
  switch {
  case !m.ghAvailable:
   footer = i18n.T(i18n.KeyPickerFooterInstallGh)
  case m.tip != "":
   footer = i18n.T(i18n.KeyPickerFooterTip, m.tip)
  }
  if footer == "" {
   return base
  }
  return base + "\n\n" + footer
 case stateMenu:
  return m.menuView()
 case stateDeleteConfirm:
  return m.deleteConfirmView()
 case stateBulkFilter:
  return m.bulkFilterView()
 case stateBulkConfirm:
  return m.bulkConfirmView()
 }
 return ""
}

func (m Model) menuView() string {
 w := m.infos[m.selIdx]
 return i18n.T(i18n.KeyPickerActionMenu, w.Branch, w.Status, w.Path)
}

func (m Model) deleteConfirmView() string {
 w := m.infos[m.selIdx]
 if w.Status == worktree.StatusPrunable {
  return m.prunableConfirmView()
 }
 cmd := fmt.Sprintf("git worktree remove %q", w.Path)
 if w.Status == worktree.StatusDirty {
  cmd = fmt.Sprintf("git worktree remove --force %q", w.Path)
 }
 return i18n.T(i18n.KeyPickerDeleteConfirm, w.Branch, w.Path, w.Status, cmd)
}

func (m Model) prunableConfirmView() string {
 var prunables []worktree.Info
 for _, in := range m.infos {
  if in.Status == worktree.StatusPrunable {
   prunables = append(prunables, in)
  }
 }
 if len(prunables) <= 1 {
  w := m.infos[m.selIdx]
  return i18n.T(i18n.KeyPickerPruneSingle, w.Branch, w.Path)
 }
 var b strings.Builder
 b.WriteString(i18n.T(i18n.KeyPickerPruneBulkHead, len(prunables)))
 for _, p := range prunables {
  b.WriteString(i18n.T(i18n.KeyPickerPruneBulkLine, p.Branch, p.Path))
 }
 b.WriteString(i18n.T(i18n.KeyPickerPruneBulkFoot))
 return b.String()
}

func (m Model) bulkFilterView() string {
 var b strings.Builder
 b.WriteString(i18n.T(i18n.KeyPickerBulkFilterHead))
 for _, s := range []worktree.Status{
  worktree.StatusPushed, worktree.StatusLocalOnly, worktree.StatusDirty,
 } {
  mark := "[ ]"
  if m.bulkFilter[s] {
   mark = "[x]"
  }
  b.WriteString(i18n.T(i18n.KeyPickerBulkFilterLine, mark, s))
 }
 b.WriteString(i18n.T(i18n.KeyPickerBulkFilterKeys))
 b.WriteString(i18n.T(i18n.KeyPickerBulkFilterFoot))
 return b.String()
}

func (m Model) bulkConfirmView() string {
 var b strings.Builder
 b.WriteString(i18n.T(i18n.KeyPickerBulkConfirmHead, len(m.bulkTargets)))
 hasDirty := HasDirty(m.infos, m.bulkTargets)
 hasPrunable := HasPrunable(m.infos, m.bulkTargets)
 dirtyStyle := lipgloss.NewStyle().Foreground(lipgloss.Color("9"))
 for _, i := range m.bulkTargets {
  w := m.infos[i]
  line := fmt.Sprintf("  %s %s  %s\n", Badge(w.Status), w.Branch, w.Path)
  if w.Status == worktree.StatusDirty && !noColor() {
   line = dirtyStyle.Render(line)
  }
  b.WriteString(line)
 }
 if hasPrunable {
  b.WriteString(i18n.T(i18n.KeyPickerBulkPruneNote))
 }
 if hasDirty {
  b.WriteString(i18n.T(i18n.KeyPickerBulkDirtyWarn))
 } else {
  b.WriteString(i18n.T(i18n.KeyPickerBulkConfirmYN))
 }
 return b.String()
}
```

- [ ] **Step 4: テスト実行**

Run: `go test ./internal/picker/ -v`
期待: すべて PASS。既存テストはステータスラベルや `--force` の存在等を検証しているだけで、英語固定モードで全部通る。

- [ ] **Step 5: コミット**

```bash
git add internal/picker/view.go internal/picker/view_test.go internal/picker/model_test.go
git commit -m "refactor(picker/view): route TUI strings through i18n"
```

---

## Task 10: picker/run.go フォールバック画面の移行

**Files:**

- Modify: `internal/picker/run.go`

- [ ] **Step 1: `internal/picker/run.go` の `runFallback` を i18n に置換**

```go
// runFallback ブロックを以下に差し替え:
func runFallback(infos []worktree.Info, in io.Reader, out io.Writer) (Action, Selection, error) {
 _, _ = fmt.Fprintln(out, i18n.T(i18n.KeyFallbackHeader))
 for i, w := range infos {
  _, _ = fmt.Fprint(out, i18n.T(i18n.KeyFallbackLine, i+1, w.Branch, w.Status, fallbackIndicators(w), w.Path))
 }
 _, _ = fmt.Fprintln(out, i18n.T(i18n.KeyFallbackNew))
 _, _ = fmt.Fprintln(out, i18n.T(i18n.KeyFallbackQuit))
 _, _ = fmt.Fprint(out, i18n.T(i18n.KeyFallbackPrompt))

 r := bufio.NewReader(in)
 line, err := r.ReadString('\n')
 if err != nil && !errors.Is(err, io.EOF) {
  return ActionCancel, Selection{}, fmt.Errorf("read choice: %w", err)
 }
 answer := strings.ToLower(strings.TrimSpace(line))
 switch answer {
 case "", "q":
  return ActionCancel, Selection{}, nil
 case "n":
  return ActionNew, Selection{}, nil
 }
 n, err := strconv.Atoi(answer)
 if err != nil || n < 1 || n > len(infos) {
  return ActionCancel, Selection{}, fmt.Errorf("invalid choice: %q", answer)
 }
 w := infos[n-1]
 return ActionResume, Selection{Path: w.Path, Branch: w.Branch, Status: w.Status, HasSession: w.HasSession}, nil
}
```

import に `"github.com/tqer39/ccw-cli/internal/i18n"` を追加。

- [ ] **Step 2: picker パッケージ全テスト実行**

Run: `go test ./internal/picker/ -v`
期待: PASS。

- [ ] **Step 3: `run_test.go` のフォールバック出力を確認するテストがあれば期待値を YAML と整合**

Run: `grep -n "Select a worktree" internal/picker/run_test.go || echo "no match"`
期待文字列が `"Select a worktree:"` のままならそのまま動く（Init("en") が走る前提なので既存値で OK）。

- [ ] **Step 4: コミット**

```bash
git add internal/picker/run.go
git commit -m "refactor(picker/run): route fallback picker strings through i18n"
```

---

## Task 11: main.go で `i18n.Init` を呼ぶ

**Files:**

- Modify: `cmd/ccw/main.go`

- [ ] **Step 1: import 追加と `main()` 修正**

`cmd/ccw/main.go` の `import` に `"github.com/tqer39/ccw-cli/internal/i18n"` を追加。
`main()` 関数を以下のように修正:

```go
func main() {
 ui.InitColor()

 flags, err := cli.Parse(os.Args[1:])
 if err != nil {
  ui.Error("%v", err)
  cli.PrintHelp(os.Stderr)
  os.Exit(2)
 }
 if err := i18n.Init(flags.Lang); err != nil {
  ui.Error("%v", err)
  os.Exit(2)
 }
 if flags.Help {
  cli.PrintHelp(os.Stdout)
  return
 }
 if flags.Version {
  fmt.Println(version.String())
  return
 }

 os.Exit(run(flags))
}
```

注意: `cli.PrintHelp` は `i18n.Init` 後に呼ばれる必要があるため `Init` を Help 処理より前に置く。
ただし `cli.Parse` 失敗時の `cli.PrintHelp(os.Stderr)` は `Init` 前なので、`PrintHelp` 内の `T()` がフォールバック（catalog 未ロード時はキー名を返す）に当たる。これを避けるため、`cli.Parse` 失敗時の help は `Init` を試してから出すよう修正:

```go
 flags, err := cli.Parse(os.Args[1:])
 if err != nil {
  // Init を best-effort で先に走らせる（失敗しても help は英語フォールバック）
  _ = i18n.Init("")
  ui.Error("%v", err)
  cli.PrintHelp(os.Stderr)
  os.Exit(2)
 }
 if err := i18n.Init(flags.Lang); err != nil {
  ui.Error("%v", err)
  os.Exit(2)
 }
```

- [ ] **Step 2: ビルド確認**

Run: `go build ./...`
期待: エラーなし。

- [ ] **Step 3: 全テスト実行**

Run: `go test ./...`
期待: すべて PASS。

- [ ] **Step 4: 手動スモークテスト — 英語**

Run（出力確認のみ・自動テストではない）:

```bash
go run ./cmd/ccw --help
```

期待: 英語のヘルプが表示される。`--lang=<en|ja>` が Options に含まれる。

- [ ] **Step 5: 手動スモークテスト — 日本語**

```bash
go run ./cmd/ccw --lang=ja --help
LANG=ja_JP.UTF-8 go run ./cmd/ccw --help
CCW_LANG=ja go run ./cmd/ccw --help
```

期待: いずれも日本語のヘルプ。

- [ ] **Step 6: 手動スモークテスト — 不正な値**

```bash
go run ./cmd/ccw --lang=fr --help
```

期待: `invalid --lang value: "fr" (expected 'en' or 'ja')` を表示して exit 2。

- [ ] **Step 7: コミット**

```bash
git add cmd/ccw/main.go
git commit -m "feat(cmd/ccw): wire i18n.Init from --lang/CCW_LANG/locale"
```

---

## Task 12: tape と README ドキュメント更新

**Files:**

- Modify: `docs/assets/picker-demo.tape`
- Modify: `README.md`
- Modify: `docs/README.ja.md`

- [ ] **Step 1: tape を English 固定に**

`docs/assets/picker-demo.tape` の冒頭（`Output` 行の直前）に追加:

```text
Env CCW_LANG en
```

正確な配置例:

```text
Output "/tmp/ccw-demo.gif"

Env CCW_LANG en

Set Shell "bash"
...
```

- [ ] **Step 2: `README.md` の Environment セクションに追記**

`README.md` を開き、既存の Environment 節（`NO_COLOR`, `CCW_DEBUG` を列挙している箇所）を探す。
そのテーブル/リストに以下を追加:

```markdown
| `CCW_LANG`  | Force output language: `en` or `ja`. Overridden by `--lang`. |
```

また Options 節（`--help` / `--version` 等）の表に以下を追加:

```markdown
| `--lang=<en\|ja>` | Force output language (overrides `CCW_LANG` and system locale). |
```

実際の README フォーマットに合わせて調整（箇条書きならその形式で）。

- [ ] **Step 3: `docs/README.ja.md` に同等の記述を追加**

同等の節に:

```markdown
| `CCW_LANG`  | 出力言語を強制: `en` または `ja`。`--lang` が優先される。 |
```

```markdown
| `--lang=<en\|ja>` | 出力言語を強制 (`CCW_LANG` および system locale より優先)。 |
```

- [ ] **Step 4: markdownlint を通すために lefthook を手動実行**

Run: `lefthook run pre-commit` （または `markdownlint-cli2 README.md docs/README.ja.md`）
期待: パス。

- [ ] **Step 5: コミット**

```bash
git add docs/assets/picker-demo.tape README.md docs/README.ja.md
git commit -m "docs: document --lang/CCW_LANG and pin demo tape to English"
```

---

## Task 13: 最終検証

**Files:**

- なし（検証のみ）

- [ ] **Step 1: 全テスト実行**

Run: `go test ./...`
期待: すべて PASS。

- [ ] **Step 2: ビルド確認**

Run: `go build ./...`
期待: エラーなし。

- [ ] **Step 3: lint 実行**

Run: `lefthook run pre-commit` または `golangci-lint run ./...`
期待: パス。

- [ ] **Step 4: 手動エンドツーエンド確認**

```bash
# English (default)
LC_ALL= LANG= CCW_LANG= go run ./cmd/ccw --help

# Japanese via system locale
LC_ALL= LANG=ja_JP.UTF-8 CCW_LANG= go run ./cmd/ccw --help

# Japanese via env var (overrides locale set to en)
LC_ALL= LANG=en_US.UTF-8 CCW_LANG=ja go run ./cmd/ccw --help

# English via flag (overrides everything)
LANG=ja_JP.UTF-8 CCW_LANG=ja go run ./cmd/ccw --lang=en --help
```

各コマンドが期待した言語のヘルプを出すことを目視確認。

- [ ] **Step 5: ブランチ状態を確認**

Run: `git log --oneline main..HEAD`
期待: 各タスクのコミットが順に並ぶ。

- [ ] **Step 6: PR 作成準備（実行は人間判断）**

`finishing-a-development-branch` skill 等で PR を作るかは別判断。
このタスクは検証完了で終了。
