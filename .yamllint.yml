extends: default

rules:
  line-length:
    max: 160
    level: warning
  truthy:
    allowed-values: ["true", "false", "on"]
    check-keys: true
  comments:
    min-spaces-from-content: 1
  document-start: disable
